<table style="width: 100%">
	<colgroup>
		<col width="50%">
		<col width="20%">
    <col width="20%">
    <col width="10%">
	<colgroup>
			
	<tr style="background-color: RoyalBlue;">
		<td>
      <h1 class="header">
        <%= link_to "Patientenübersicht", karteikarten_path(), :class => current_page?(karteikarten_path()) ? "current" : "header" %> &nbsp;&nbsp;  
		    <%= link_to "Abfragen", abfragen_path(), :class => current_page?(abfragen_path()) ? "current" : "header" %>
      </h1>
    </td>
    <td>
      <%#= yield :header %>
    </td>
    <td>
      <% if signed_in? %>
        <h1 style="color: #FBFBB1; font-weight: normal" class="header">Willkommen <%= current_user.username %></h1>
      <% end %>
    </td>
		<td style="text-align: right">
      <% if signed_in? %>
        <h1 class="header"><%= link_to "abmelden", logout_path(), :method => :delete, :class => current_page?(logout_path()) ? "current" : "header" %></h1>
      <% else %>
        <h1 class="header"><%= link_to "anmelden", login_path(), :class => "current" %></h1>
      <% end %>
    </td>
	</tr>
</table>
